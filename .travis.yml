language: rust

rust:
  - stable
  - beta
  - nightly

sudo: false

notifications:
    email:
        on_success: never
        on_failure: always

matrix:
  allow_failures:
    - rust: nightly

script:
    - cargo build
    - cargo test
    - cargo doc --no-deps
    - rustdoc --test README.md -L target
